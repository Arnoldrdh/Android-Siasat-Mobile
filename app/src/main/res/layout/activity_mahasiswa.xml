<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#F1F5F9">

    <!-- Guideline untuk turunkan semua konten -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/topGuideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.15" />

    <!-- Nama Mahasiswa -->
    <TextView
        android:id="@+id/tvNamaMahasiswa"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Halo, Mahasiswa 👋"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#334155"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="@id/topGuideline"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Tombol Logout -->
    <Button
        android:id="@+id/btnLogout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:backgroundTint="#94A3B8"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tvNamaMahasiswa"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Judul -->
    <TextView
        android:id="@+id/tvJudulMatkul"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Mata Kuliah Diambil"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#0F172A"
        android:gravity="center"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/btnLogout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/btnAmbilMatkul"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ambil Mata Kuliah"
        android:textAllCaps="false"
        android:backgroundTint="#0F172A"
        android:textColor="#FFFFFF"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tvJudulMatkul"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <!-- ScrollView -->
    <ScrollView
        android:id="@+id/scrollMahasiswaMatkul"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="12dp"
        android:textColor="#0F172A"
        app:layout_constraintTop_toBottomOf="@id/btnAmbilMatkul"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/containerMahasiswaMatkul"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="32dp" />
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
